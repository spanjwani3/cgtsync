# ----- Supabase -----
# Found in: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>

# Service role key — used ONLY server-side for storage operations and admin tasks.
# Found in: Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# Supabase URL (server-side, same as NEXT_PUBLIC_SUPABASE_URL)
SUPABASE_URL=https://<project-ref>.supabase.co

# ----- Database (Prisma) -----
# Found in: Supabase Dashboard → Project Settings → Database → Connection string (URI)
# Use the "Transaction" pooler URI (port 6543) for serverless; direct (port 5432) for migrations.
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DIRECT_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# ----- App -----
# Production URL — used for auth redirects. Set to your Vercel domain in production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ----- Anthropic (server only, for AI extraction) -----
ANTHROPIC_API_KEY=<your-anthropic-api-key>
# Optional: override default model (defaults to claude-sonnet-4-5-20250929)
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# ----- Email (Resend) -----
# Required for sending confirmation emails, reminders, and dispute packs.
RESEND_API_KEY=<your-resend-api-key>
# Optional: custom sender address (defaults to notifications@cgtsync.com)
# RESEND_FROM_EMAIL=notifications@yourdomain.com
# Recommended: webhook signature secret for verifying Resend webhook events
# RESEND_WEBHOOK_SECRET=<your-webhook-secret>

# ----- Storage (optional override) -----
# SUPABASE_EVIDENCE_BUCKET=evidence
